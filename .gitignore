# runtime data — not source code
data/
*.db

# host-mounted image directories
profile-images/
bg-images/
widget-images/

# build artifacts
muhomu

# static data files are source code, not runtime data
!static/data/
!static/data/*
